def arithmetic_arranger(problems):
    # Justify len(problem) > 5, then return Too many problem
    if len(problems) > 5:
        return "Error: Too many problems."
    str
    # 1. Separate list
    for lst in problems:
    # 2. put splitted variable into line
    
        
    return arranged_problems
